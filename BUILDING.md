# Building BookieTrader V0.1

Install the .NET 10 SDK, then run:

```powershell
dotnet restore
dotnet build BookieTrader.csproj -c Release
```

A successful Release build creates:

`ReleaseZip\Michael-BookieTrader-0.1.0.zip`
